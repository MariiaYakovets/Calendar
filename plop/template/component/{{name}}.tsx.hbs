import React from "react";
import * as styles from './{{name}}.module.css' 

interface I{{name}}Props {

}

export default function {{name}}(props: I{{name}}Props ) {

    const {} = props

    return ( 
        <{{tag}} className = { styles.{{name}} }>

        </{{tag}}> 
    );
}

